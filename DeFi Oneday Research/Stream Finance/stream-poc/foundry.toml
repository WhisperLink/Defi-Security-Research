[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true
evm_version = "cancun"

# Stream Finance xUSD collapse reproduction (Arbitrum fork)
[rpc_endpoints]
arbitrum = "${ARBITRUM_RPC_URL}"
