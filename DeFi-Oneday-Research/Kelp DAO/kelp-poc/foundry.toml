[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true
evm_version = "cancun"

# Kelp DAO rsETH bridge exploit (2026-04-18) reproduction fork
[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
